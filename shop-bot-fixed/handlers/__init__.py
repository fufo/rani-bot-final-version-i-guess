"""
Handler modules:
  callback_router     — main callback dispatcher
  user_handlers       — /start, shop, buy, profile
  admin_handlers      — admin panel, products, users
  message_handlers    — text/file input wizard flows
  settings_handlers   — bot settings panel
  admin_management_handlers
  force_subscribe_handlers
  referral_admin_handlers
  stars_admin_handlers
  reward_handlers     — reward links, daily gift
  rating_handlers     — product reviews (NEW v3.1)
  delivery_handlers   — smart delivery, retry, logs (NEW v3.1)
"""
